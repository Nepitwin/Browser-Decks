<UserControl x:Class="BrowserDecks.Controls.WebBrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:valueConverters="clr-namespace:ValueConverters;assembly=ValueConverters"
             mc:Ignorable="d"
             x:Name="wbc"
             KeyUp="OnKeyUp"
             d:DataContext="{d:DesignInstance }">
    <UserControl.Resources>
        <ResourceDictionary>
            <valueConverters:BoolToVisibilityConverter x:Key="VisibilityConverter" TrueValue="Visible" FalseValue="Collapsed"/>
        </ResourceDictionary>
</UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Visibility="{Binding Path=IsTitleBarVisible, ElementName=wbc, Converter={StaticResource VisibilityConverter}}" Text="{Binding Path=WebUri, ElementName=wbc}" KeyUp="OnKeyUp"/>
        <wv2:WebView2 Grid.Row="1" Source="{Binding Path=WebUri, Mode=TwoWay, ElementName=wbc}"/>
    </Grid>
</UserControl>
